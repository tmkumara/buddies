spring:
  datasource:
    url: jdbc:mysql://mysql:3306/giftbox_oms?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: giftbox
    password: giftbox
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false

  flyway:
    enabled: false   # set true later when we add migrations
